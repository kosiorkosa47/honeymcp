/target
/data
*.db
*.db-journal
*.db-wal
*.db-shm
/logs
*.jsonl
.DS_Store

# Real canary token values (Thinkst canarytokens.org). NEVER commit — the
# committed canaries.example.yaml documents the shape with dummy values.
canaries.yaml
canaries.toml
/shots/

# Local tooling state, never commit
.claude/
.context-memory/
.context-memory-backup-*/
.local-plans/
CLAUDE.md
.cursor/
.vscode/
.idea/
*.tmp
